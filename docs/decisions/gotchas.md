# Known Gotchas

A running list of non-obvious project quirks. Claude reads this file to avoid known pitfalls.
Add entries as you discover them — format: `- description of the gotcha`

<!-- Example entries (delete these when you add real ones):
- The `users` table has a `deleted_at` column but we use `is_active` boolean for filtering
- Hot reload breaks when editing `middleware.ts` — restart the dev server
-->
