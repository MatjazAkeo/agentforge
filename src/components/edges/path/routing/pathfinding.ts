import type { PathInput, Point } from './types';

export function routeWithPathfinding(_input: PathInput): Point[] {
  throw new Error('routeWithPathfinding: not implemented yet (deferred to a future release)');
}
